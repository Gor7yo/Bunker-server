# 🔧 Быстрая настройка для IP: 195.80.51.69

## 📍 Текущая ситуация
- **IP сервера:** 195.80.51.69
- **Проект уже развернут** (директория `server` может быть в другом месте)

## 🔍 ШАГ 1: Найти где находится проект

Подключись к серверу:
```bash
ssh root@195.80.51.69
```

Найди где находится проект:
```bash
# Ищем файл websocket.js
find / -name "websocket.js" 2>/dev/null

# Или ищем package.json
find / -name "package.json" -path "*/server/*" 2>/dev/null

# Или проверяем стандартные места
ls -la /opt/
ls -la /root/
ls -la /home/
```

**Скорее всего проект находится в:**
- `/opt/bunker-server/server/`
- `/root/bunker-server/server/`
- `/root/server/`
- `/home/ваш-пользователь/bunker-server/server/`

## 📂 ШАГ 2: Перейти в директорию проекта

После того как нашел, перейди туда:
```bash
cd /путь/к/проекту/server
# Например: cd /opt/bunker-server/server
# или: cd /root/bunker-server/server

# Проверь что ты в правильной директории
pwd
ls -la
# Должны быть файлы: websocket.js, package.json, ecosystem.config.js
```

## 🔨 ШАГ 3: Установить зависимости (если еще не установлены)

```bash
# Проверь есть ли node_modules
ls -la node_modules/

# Если нет - установи
pnpm install
# или
npm install
```

## 🚀 ШАГ 4: Запустить сервер

```bash
# Создай директорию для логов (если нет)
mkdir -p logs

# Запусти через PM2
pm2 start ecosystem.config.js

# Сохрани конфигурацию
pm2 save

# Настрой автозапуск (уже сделано, но на всякий случай)
pm2 startup
```

## ✅ ШАГ 5: Проверить работу

```bash
# Статус
pm2 status

# Логи
pm2 logs bunker-server

# Проверка порта
ss -tulpn | grep 5000
```

**Должно показать:**
- `pm2 status` - процесс `bunker-server` в статусе `online`
- Логи показывают: `✅ Сервер запущен на порту 5000`
- Порт 5000 слушается

## 🔥 ШАГ 6: Открыть порт в firewall

```bash
# Проверь статус firewall
ufw status

# Если не включен или порт не открыт:
ufw allow 5000/tcp
ufw enable
ufw status
```

## 🌐 ШАГ 7: Обновить клиент

### В файле `client/src/pages/JoinRoom/JoinRoom.jsx` (строка ~28):
```javascript
const socket = new WebSocket("ws://195.80.51.69:5000");
```

### В файле `client/src/pages/AdminPanel/AdminPanel.jsx` (строка ~24):
```javascript
const wsUrl = 'ws://195.80.51.69:5000';
```

## 🧪 ШАГ 8: Протестировать

1. Запусти клиент локально
2. Попробуй подключиться к серверу
3. Проверь логи на сервере: `pm2 logs bunker-server`

## 🆘 Если что-то не работает

### Сервер не запускается:
```bash
# Смотри ошибки
pm2 logs bunker-server --err

# Проверь что файлы на месте
ls -la websocket.js package.json ecosystem.config.js
```

### Порт занят:
```bash
# Найди что занимает порт
lsof -i :5000
# или
ss -tulpn | grep 5000

# Если занят другим процессом - останови его или измени порт в ecosystem.config.js
```

### Не могу найти проект:
```bash
# Поиск по всем директориям
find / -type f -name "websocket.js" 2>/dev/null
find / -type f -name "ecosystem.config.js" 2>/dev/null
```

---

## 📝 Полезные команды

```bash
# Просмотр статуса
pm2 status

# Просмотр логов
pm2 logs bunker-server
pm2 logs bunker-server --lines 50

# Перезапуск
pm2 restart bunker-server

# Остановка
pm2 stop bunker-server

# Мониторинг ресурсов
pm2 monit
```


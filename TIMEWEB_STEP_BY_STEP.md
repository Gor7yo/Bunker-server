# 📋 Пошаговая инструкция: Деплой на TimeWeb VPS

## 🎯 ШАГ 1: Регистрация и заказ VPS на TimeWeb

### 1.1. Регистрация на TimeWeb
1. Перейди на сайт: **https://timeweb.com/ru/**
2. Нажми кнопку **"Регистрация"** или **"Войти"** (если уже есть аккаунт)
3. Заполни форму регистрации:
   - Email
   - Пароль
   - Подтверждение пароля
4. Подтверди email (проверь почту)

### 1.2. Заказ VPS
1. В личном кабинете TimeWeb перейди в раздел **"VPS"** или **"Виртуальные серверы"**
2. Нажми **"Заказать VPS"** или **"Создать сервер"**
3. Выбери тариф:
   - **Минимум для P2P (8 игроков):** VPS-1 (1 vCPU, 1 GB RAM, 20 GB SSD)
   - **С запасом (рекомендуется):** VPS-2 (2 vCPU, 2 GB RAM, 40 GB SSD)
4. Выбери операционную систему:
   - **Ubuntu 22.04 LTS** (рекомендуется)
   - или Ubuntu 20.04 LTS
5. Выбери регион (ближайший к тебе)
6. Нажми **"Заказать"** или **"Создать"**
7. Дождись создания сервера (обычно 5-10 минут)

### 1.3. Получение данных для доступа
После создания сервера в панели TimeWeb ты увидишь:
- **IP-адрес сервера** (например: `185.123.45.67`)
- **Логин** (обычно `root`)
- **Пароль** (сохрани его!)

**Важно:** Сохрани эти данные в безопасном месте!

---

## 🔐 ШАГ 2: Подключение к серверу

### 2.1. Через Windows (PowerShell или CMD)

#### Вариант A: Встроенный SSH (Windows 10/11)
```powershell
# Открой PowerShell или CMD
ssh root@YOUR_SERVER_IP
# Например: ssh root@185.123.45.67
```

#### Вариант B: PuTTY (если SSH не работает)
1. Скачай PuTTY: https://www.putty.org/
2. Установи и запусти
3. В поле "Host Name" введи IP сервера
4. Port: `22`
5. Connection type: `SSH`
6. Нажми "Open"
7. Введи логин: `root`
8. Введи пароль (при вводе пароль не отображается - это нормально)

### 2.2. Первое подключение
При первом подключении появится предупреждение:
```
The authenticity of host '185.123.45.67' can't be established.
Are you sure you want to continue connecting (yes/no)?
```
Введи `yes` и нажми Enter.

### 2.3. Ввод пароля
Введи пароль от сервера (символы не отображаются - это нормально) и нажми Enter.

**Если подключился успешно, увидишь:**
```
root@server-name:~#
```

---

## 📦 ШАГ 3: Обновление системы

Выполни команды по порядку:

```bash
# Обновляем список пакетов
apt update

# Обновляем установленные пакеты
apt upgrade -y

# Устанавливаем необходимые утилиты
apt install -y curl wget git ufw
```

**Ожидай завершения** (может занять 2-5 минут).

---

## 🟢 ШАГ 4: Установка Node.js

### 4.1. Добавляем репозиторий Node.js
```bash
# Добавляем репозиторий Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
```

### 4.2. Устанавливаем Node.js
```bash
apt install -y nodejs
```

### 4.3. Проверяем установку
```bash
node -v
npm -v
```

**Должно показать:**
```
v20.x.x
10.x.x
```

Если версии отображаются - отлично! ✅

---

## 📥 ШАГ 5: Установка PM2 и pnpm

```bash
# Устанавливаем PM2 глобально (для управления процессом)
npm install -g pm2

# Устанавливаем pnpm глобально (менеджер пакетов)
npm install -g pnpm

# Проверяем установку
pm2 -v
pnpm -v
```

**Должно показать версии** - значит всё установлено! ✅

---

## 📂 ШАГ 6: Загрузка проекта на сервер

### Вариант A: Через Git (если проект в Git)

```bash
# Переходим в директорию /opt
cd /opt

# Клонируем репозиторий (замени URL на свой)
git clone https://github.com/your-username/your-repo.git bunker-server

# Переходим в директорию сервера
cd bunker-server/server
```

### Вариант B: Через SCP (если нет Git)

**На твоем компьютере (Windows PowerShell):**

```powershell
# Перейди в папку с проектом
cd "C:\Users\gorce\OneDrive\Desktop\Bunker 2"

# Создай архив сервера
tar -czf server.tar.gz server/

# Загрузи на сервер (замени IP на свой)
scp server.tar.gz root@YOUR_SERVER_IP:/opt/
```

**На сервере:**

```bash
# Переходим в /opt
cd /opt

# Распаковываем архив
tar -xzf server.tar.gz

# Переименовываем (если нужно)
mv server bunker-server
cd bunker-server/server
```

### Вариант C: Через WinSCP (графический интерфейс)

1. Скачай WinSCP: https://winscp.net/
2. Установи и запусти
3. Подключись к серверу:
   - Host name: IP сервера
   - User name: `root`
   - Password: пароль сервера
4. Перетащи папку `server` в `/opt/bunker-server/`

**На сервере:**
```bash
cd /opt/bunker-server/server
```

---

## 🔨 ШАГ 7: Установка зависимостей проекта

```bash
# Убедись, что ты в директории server
pwd
# Должно показать: /opt/bunker-server/server (или твой путь)

# Устанавливаем зависимости
pnpm install
```

**Это займет 2-5 минут.** Дождись завершения.

Если `pnpm` не работает, используй:
```bash
npm install
```

---

## 📁 ШАГ 8: Создание директории для логов

```bash
mkdir -p logs
```

---

## 🚀 ШАГ 9: Запуск сервера через PM2

```bash
# Запускаем сервер
pm2 start ecosystem.config.js

# Сохраняем конфигурацию PM2
pm2 save

# Настраиваем автозапуск при перезагрузке
pm2 startup
```

**Примечание:** Если ты работаешь от пользователя `root`, команда `pm2 startup` выполнится автоматически без дополнительных команд. Если работаешь от обычного пользователя, появится команда с `sudo env PATH=...` - её нужно будет выполнить.

### Проверяем статус:
```bash
pm2 status
```

**Должно показать:**
```
┌─────┬─────────────────┬─────────┬─────────┬──────────┐
│ id  │ name            │ status  │ restart │ uptime   │
├─────┼─────────────────┼─────────┼─────────┼──────────┤
│ 0   │ bunker-server   │ online  │ 0       │ 5s       │
└─────┴─────────────────┴─────────┴─────────┴──────────┘
```

### Смотрим логи:
```bash
pm2 logs bunker-server
```

**Должно показать:**
```
✅ Сервер запущен на порту 5000
🚀 Сервер 'Бункер' готов для 8 игроков!
```

Если видишь эти сообщения - сервер работает! ✅

**Выйти из логов:** Нажми `Ctrl + C`

---

## 🔥 ШАГ 10: Настройка Firewall

```bash
# Разрешаем SSH (чтобы не потерять доступ)
ufw allow 22/tcp

# Разрешаем порт сервера
ufw allow 5000/tcp

# Включаем firewall
ufw enable

# Проверяем статус
ufw status
```

**Должно показать:**
```
Status: active

To                         Action      From
--                         ------      ----
22/tcp                     ALLOW       Anywhere
5000/tcp                   ALLOW       Anywhere
```

---

## ✅ ШАГ 11: Проверка работы сервера

### 11.1. Проверка порта
```bash
ss -tulpn | grep 5000
```

**Должно показать:**
```
tcp   LISTEN  0  128  0.0.0.0:5000  0.0.0.0:*  users:(("node",pid=1234))
```

### 11.2. Проверка из браузера
Открой в браузере:
```
http://YOUR_SERVER_IP:5000
```

Если сервер работает, увидишь ошибку подключения WebSocket (это нормально - нужен клиент).

### 11.3. Проверка логов
```bash
pm2 logs bunker-server --lines 20
```

---

## 🌐 ШАГ 12: Обновление клиента (на твоем компьютере)

После того как сервер запущен, нужно обновить адрес в клиенте.

### 12.1. Открой файл `client/src/pages/JoinRoom/JoinRoom.jsx`

Найди строку 28 и замени:
```javascript
// БЫЛО:
const socket = new WebSocket("wss://bunker-server-ujdj.onrender.com");

// СТАЛО (замени IP на свой):
const socket = new WebSocket("ws://YOUR_SERVER_IP:5000");
// Например: const socket = new WebSocket("ws://185.123.45.67:5000");
```

### 12.2. Открой файл `client/src/pages/AdminPanel/AdminPanel.jsx`

Найди строку 24 и замени:
```javascript
// БЫЛО:
const wsUrl = 'wss://bunker-server-ujdj.onrender.com';

// СТАЛО (замени IP на свой):
const wsUrl = 'ws://YOUR_SERVER_IP:5000';
// Например: const wsUrl = 'ws://185.123.45.67:5000';
```

### 12.3. Пересобери и запусти клиент
```powershell
cd client
pnpm install
pnpm run build
# или для разработки:
pnpm run dev
```

---

## 🎉 Готово!

Теперь твой сервер работает на TimeWeb VPS!

### Полезные команды для управления:

```bash
# Просмотр статуса
pm2 status

# Просмотр логов
pm2 logs bunker-server

# Перезапуск
pm2 restart bunker-server

# Остановка
pm2 stop bunker-server

# Мониторинг ресурсов
pm2 monit
```

### Если что-то пошло не так:

1. **Сервер не запускается:**
   ```bash
   pm2 logs bunker-server --err
   ```

2. **Порт занят:**
   ```bash
   lsof -i :5000
   ```

3. **Недостаточно памяти:**
   ```bash
   free -h
   pm2 monit
   ```

---

## 📞 Следующие шаги (опционально)

- Настройка домена (см. `TIMEWEB_DEPLOY.md`, раздел "Настройка домена")
- Настройка SSL (HTTPS/WSS)
- Настройка Nginx как reverse proxy

**Удачи! 🚀**



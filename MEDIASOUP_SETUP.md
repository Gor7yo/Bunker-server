# Настройка Mediasoup на Render.com

## Важно: Selectel НЕ нужен!

Mediasoup работает на том же сервере, где и WebSocket сервер (Render.com). Отдельный сервер не требуется.

## 1. Настройка на Render.com

### Переменные окружения в Render.com Dashboard:

1. Перейдите в настройки вашего сервиса на Render.com
2. Добавьте переменные окружения:

```env
PORT=5000
NODE_ENV=production
# MEDIASOUP_ANNOUNCED_ADDRESS - домен вашего сервиса на Render.com
# По умолчанию: bunker-server-ujdj.onrender.com
# Если используете другой домен, укажите его здесь
```

### Порты на Render.com:

Render.com автоматически обрабатывает порты через переменную `PORT`. 
Для медиа-трафика (UDP 40000-49999) Render.com использует тот же внешний IP.

**Важно:** На Render.com не нужно открывать порты вручную - они открываются автоматически.

## 2. Особенности Render.com

### Автоматическое определение IP:
- Render.com автоматически определяет внешний IP
- Обычно `MEDIASOUP_ANNOUNCED_IP` не нужен
- Если есть проблемы, можно указать IP вручную (найдите его в логах Render)

### Ограничения:
- Render.com может иметь ограничения на UDP трафик
- Если медиа не работает, попробуйте использовать TCP fallback (уже включен в коде)

## 3. Проверка работы

1. После деплоя на Render.com проверьте логи:
   - Должно быть: "✅ Mediasoup инициализирован"
   - Должно быть: "🚀 Сервер 'Бункер' готов для 8 игроков!"

2. Проверьте подключение клиентов:
   - Откройте консоль браузера
   - Должны быть сообщения о создании транспортов и producers

## 4. Troubleshooting

### Проблема: "Transport connection failed"
- Проверьте, что сервер запущен на Render.com
- Проверьте логи на Render.com
- Убедитесь, что WebSocket URL правильный: `wss://bunker-server-ujdj.onrender.com`

### Проблема: "Worker died"
- Проверьте логи на Render.com
- Убедитесь, что достаточно памяти (Render.com может ограничивать)
- Проверьте, что все зависимости установлены

### Проблема: Нет видео/аудио
- Проверьте консоль браузера
- Убедитесь, что разрешения камеры/микрофона выданы
- Проверьте, что клиент использует `useMediasoup` (не `useWebRTC`)

## 5. Оптимизация для Render.com

Render.com может иметь ограничения на ресурсы. Для лучшей работы:

1. Используйте более низкие битрейты (уже настроено в коде)
2. Мониторьте использование памяти через Render.com Dashboard
3. При необходимости увеличьте план на Render.com

## 6. Альтернатива: Если Render.com не подходит для медиа

Если Render.com не справляется с медиа-трафиком, можно:
- Использовать гибридный подход: WebSocket на Render.com, медиа на Selectel
- Но это сложнее и обычно НЕ нужно - Render.com должен справиться

**Рекомендация:** Сначала попробуйте на Render.com. Если будут проблемы - тогда можно рассмотреть Selectel для медиа.

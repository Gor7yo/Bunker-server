# 🎥 Включение Mediasoup на сервере

## ✅ Что уже сделано:

1. ✅ Клиент переключен на `useMediasoup` вместо `useWebRTC`
2. ✅ Сервер настроен на использование `api.bunker-server.ru` для mediasoup
3. ✅ `ecosystem.config.js` обновлен с `USE_MEDIASOUP: 'true'`

## 🚀 Шаги для активации на сервере:

### 1. Обнови файлы на сервере

```bash
# Подключись к серверу
ssh root@195.80.51.69

# Перейди в директорию проекта
cd /opt/bunker-server/server

# Загрузи обновленные файлы:
# - ecosystem.config.js (с USE_MEDIASOUP: 'true')
# - websocket.js (с api.bunker-server.ru)
```

### 2. Убедись что mediasoup worker собран

```bash
# Проверь наличие worker
ls -la node_modules/.pnpm/mediasoup@*/node_modules/mediasoup/worker/out/Release/mediasoup-worker

# Если worker не найден - собери его через скрипт
cd /opt/bunker-server/server
npm run build:mediasoup

# Или вручную:
cd node_modules/.pnpm/mediasoup@*/node_modules/mediasoup/worker
npm install
```

### 3. Перезапусти сервер

```bash
# Перезапусти PM2
pm2 restart bunker-server

# Проверь логи
pm2 logs bunker-server
```

**Ожидаемый вывод:**
```
✅ Сервер запущен на порту 5000
🚀 Сервер 'Бункер' готов для 8 игроков!
✅ Mediasoup активен - медиа-функции доступны
```

### 4. Проверь что mediasoup работает

```bash
# Смотри логи в реальном времени
pm2 logs bunker-server --lines 50

# Проверь что нет ошибок инициализации
```

## 🔧 Настройка портов для mediasoup

Mediasoup использует UDP порты для RTC. Убедись что они открыты:

```bash
# Открой диапазон портов для RTC (40000-49999)
ufw allow 40000:49999/udp
ufw allow 40000:49999/tcp

# Проверь статус
ufw status
```

**Также в панели TimeWeb:**
- Открой UDP порты 40000-49999 (если есть firewall в панели)

## 📝 Проверка работы

1. **На сервере:**
   ```bash
   pm2 logs bunker-server
   # Должно быть: "✅ Mediasoup активен"
   ```

2. **В клиенте:**
   - Открой консоль браузера (F12)
   - Должно быть: "✅ Mediasoup device инициализирован"
   - Видео должно работать через mediasoup

## 🆘 Решение проблем

### Ошибка: "Mediasoup не инициализирован"
```bash
# Проверь что worker собран
ls -la node_modules/.pnpm/mediasoup@*/node_modules/mediasoup/worker/out/Release/mediasoup-worker

# Если нет - собери
cd node_modules/.pnpm/mediasoup@*/node_modules/mediasoup
npm run build:worker
```

### Ошибка: "spawn mediasoup-worker ENOENT"
```bash
# Worker не собран или не найден
# Собери worker (см. выше)
```

### Видео не работает
- Проверь логи клиента (F12 → Console)
- Проверь логи сервера: `pm2 logs bunker-server`
- Убедись что порты 40000-49999 открыты

## ✅ Готово!

После выполнения всех шагов mediasoup должен работать. Клиент автоматически использует mediasoup вместо P2P.


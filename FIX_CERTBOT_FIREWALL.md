# 🔥 Быстрое решение проблемы Certbot: Firewall

## ❌ Ошибка:
```
Timeout during connect (likely firewall problem)
```

## ✅ Решение (по порядку):

### ШАГ 1: Открой порты в UFW на сервере

```bash
# Проверь текущий статус firewall
ufw status

# Открой порты HTTP и HTTPS
ufw allow 80/tcp
ufw allow 443/tcp

# Проверь что порты открыты
ufw status
```

**Должно показать:**
```
Status: active

To                         Action      From
--                         ------      ----
80/tcp                     ALLOW       Anywhere
443/tcp                    ALLOW       Anywhere
```

---

### ШАГ 2: Открой порты в панели TimeWeb

**Важно!** TimeWeb может иметь свой firewall в панели управления:

1. Зайди в личный кабинет TimeWeb: https://timeweb.com/ru/
2. Найди свой VPS сервер
3. Открой раздел **"Firewall"**, **"Брандмауэр"** или **"Сеть"**
4. Добавь правила:
   - **Порт:** `80` (HTTP) → **Действие:** Разрешить
   - **Порт:** `443` (HTTPS) → **Действие:** Разрешить
5. Сохрани изменения

**Если не можешь найти firewall в панели:**
- Проверь документацию TimeWeb
- Или обратись в поддержку TimeWeb

---

### ШАГ 3: Проверь что Nginx работает

```bash
# Проверь статус Nginx
systemctl status nginx

# Если не запущен - запусти
systemctl start nginx
systemctl enable nginx

# Проверь что порт 80 слушается
ss -tulpn | grep :80
```

**Должно показать:**
```
tcp   LISTEN  0  128  0.0.0.0:80  0.0.0.0:*  users:(("nginx",pid=1234))
```

---

### ШАГ 4: Проверь доступность извне

Открой в браузере (с любого компьютера):
```
http://api.bunker-server.ru
```

**Должно показать:**
- Либо ошибку WebSocket (это нормально - сервер работает)
- Либо какую-то ошибку от сервера (но НЕ "Сайт недоступен")

**Если показывает "Сайт недоступен"** - порт 80 все еще заблокирован.

---

### ШАГ 5: Повтори получение SSL

```bash
# Повтори команду
certbot --nginx -d api.bunker-server.ru
```

Если все настроено правильно - SSL должен установиться успешно!

---

## 🔍 Дополнительная диагностика

### Проверка портов извне:
```bash
# С другого компьютера проверь доступность порта 80
telnet api.bunker-server.ru 80
# или
nc -zv api.bunker-server.ru 80
```

### Проверка логов Nginx:
```bash
# Смотри логи ошибок
tail -f /var/log/nginx/error.log

# Смотри логи доступа
tail -f /var/log/nginx/access.log
```

### Проверка конфигурации Nginx:
```bash
# Проверь синтаксис
nginx -t

# Перезагрузи конфигурацию
systemctl reload nginx
```

---

## 💡 Альтернативное решение: Standalone режим

Если проблема с firewall не решается, можно использовать standalone режим (но нужно временно остановить Nginx):

```bash
# Останови Nginx
systemctl stop nginx

# Получи сертификат в standalone режиме
certbot certonly --standalone -d api.bunker-server.ru

# Запусти Nginx обратно
systemctl start nginx

# Настрой Nginx вручную для использования сертификата
# (см. конфигурацию в TIMEWEB_SSL_SETUP.md)
```

---

## ✅ После успешного получения SSL

Проверь что все работает:
```bash
# Проверь сертификат
certbot certificates

# Проверь в браузере
# https://api.bunker-server.ru - должен быть замочек 🔒
```

